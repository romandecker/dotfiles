# lang: bash
# -a => archive mode, i.e. preserve timestamps etc.
# -r => recursive
# -P => show progress info
# --delete => delete files from dest that are not in source

rsync -arP src dest

rsync -rP src dest    # creates a directory "dest/src"

rsync -rP src/ dest   # copies all files in "src" directly into "dest"