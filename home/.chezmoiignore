# Files that live in the source tree but must NOT be applied into $HOME.
#
# The Brewfile is read by the package-install script via `include "Brewfile"`
# (so its hash triggers re-runs), but it is not itself a dotfile.
Brewfile
