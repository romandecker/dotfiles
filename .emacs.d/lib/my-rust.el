;;; package --- My custom config for editing RUST source
;;; Commentary:
;;; Code:

(use-package rust-mode
  :ensure t
  :config
  (setq rust-format-on-save t))


(provide 'my-rust)
;;; my-rust.el ends here
