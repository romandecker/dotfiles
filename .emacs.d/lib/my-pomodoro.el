;;; package --- My custom config for doing pomodoros
;;; Commentary:
;;; Code:

(use-package tomatinho
  :ensure t
  :config
  (setq tomatinho-pomodoro-length 1))

(provide 'my-pomodoro)
;;; my-pomodoro.el ends here
